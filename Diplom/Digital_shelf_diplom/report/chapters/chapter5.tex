\setcounter{chaptercntr}{5}

\sectionbreak \section*{
	\gostTitleFont
	\redline
	\thechaptercntr .
	РАЗВЁРТЫВАНИЕ СИСТЕМЫ «RETAIL VISION»
}

\titlespace

\subsection*{
	\gostTitleFont
	\redline
	\thechaptercntr .\thesubchaptercntr \spc
	Выбор стратегии развёртывания системы
} \addtocounter{subchaptercntr}{1}

\subtitlespace

{\gostFont

	\par \redline Развёртывание системы является завершающим этапом разработки, определяющим доступность и стабильность работы продукта в производственной среде. Для системы «Retail Vision» необходимо обеспечить круглосуточную доступность, низкую задержку обработки запросов и возможность одновременного обслуживания нескольких пользователей-мерчендайзеров.

	\par \redline С архитектурной точки зрения система состоит из четырёх взаимозависимых компонентов: серверного приложения Telegram-бота на базе aiogram, модуля YOLOv8n, базы данных планограмм и файлового хранилища отчётов. Взаимодействие компонентов системы представлено на рисунке \thechaptercntr .\theimagecntr.

	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{images/bot_architecture.png}
		\caption*{\gostFont Рисунок \thechaptercntr .\theimagecntr \spc {--} Архитектурная схема системы «Retail Vision».}
		\label{fig:bot_arch}
	\end{figure} \addtocounter{imagecntr}{1}

	\par \redline Существуют два основных подхода к взаимодействию Telegram-бота с серверами Telegram: \textbf{polling} (периодический опрос сервера) и \textbf{webhook} (получение обновлений по событию). При polling-режиме бот с фиксированным интервалом запрашивает сервер Telegram на наличие новых сообщений. Такой подход прост в реализации, однако создаёт постоянную нагрузку на канал передачи данных и приводит к задержкам обработки, пропорциональным интервалу опроса. При webhook-режиме Telegram самостоятельно отправляет HTTP POST-запрос на зарегистрированный URL при поступлении нового сообщения, что обеспечивает минимальную задержку и исключает избыточный трафик.

	\par \redline С учётом требования к низкой задержке обработки изображений в рамках производственного аудита для системы «Retail Vision» выбран \textbf{webhook}-режим. Это решение позволяет гарантировать время реакции системы на входящее сообщение, не зависящее от интервала опроса.

	\par \redline Для развёртывания серверной инфраструктуры рассматривались следующие варианты:

	\begin{itemize}[leftmargin=2.15cm, labelwidth=0.65cm, labelsep=0.0cm]

		\item[\theitemcntr. ] Физический сервер в локальной сети предприятия.
		\addtocounter{itemcntr}{1}

		\item[\theitemcntr. ] Виртуальная машина у облачного провайдера (VPS/VDS).
		\addtocounter{itemcntr}{1}

		\item[\theitemcntr. ] Бессерверное развёртывание (Serverless, FaaS).
		\addtocounter{itemcntr}{1}

	\end{itemize} \setcounter{itemcntr}{1}

	\par \redline Физический сервер требует значительных капитальных затрат на оборудование, обеспечение резервирования питания и организацию защищённого удалённого доступа. Бессерверное развёртывание предполагает «холодный старт» контейнера при каждом вызове, что недопустимо при необходимости загрузки модели YOLOv8n (размер файла весов $\approx 6$ МБ) перед обработкой каждого запроса. Таким образом, оптимальным решением является аренда \textbf{виртуальной машины (VPS)} у облачного провайдера. Данный подход обеспечивает постоянную доступность, предсказуемые характеристики производительности, возможность гибкого масштабирования и полный контроль над программной конфигурацией сервера.

	\par \redline На рисунке \thechaptercntr .\theimagecntr показана схема выбора стратегии развёртывания в виде дерева решений, иллюстрирующая обоснование перехода к VPS с webhook-режимом.

	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{images/deployment_tree.png}
		\caption*{\gostFont Рисунок \thechaptercntr .\theimagecntr \spc {--} Дерево принятия решений при выборе стратегии развёртывания.}
		\label{fig:deployment_tree}
	\end{figure} \addtocounter{imagecntr}{1}

	\par
}

\subtitlespace

\subsection*{
	\gostTitleFont
	\redline
	\thechaptercntr .\thesubchaptercntr \spc
	Конвертация и оптимизация модели YOLOv8n для развёртывания
} \addtocounter{subchaptercntr}{1}

\subtitlespace

{\gostFont

	\par \redline Модель YOLOv8n, обученная в формате PyTorch (\texttt{.pt}), перед развёртыванием конвертируется в формат \textbf{ONNX} (Open Neural Network Exchange). ONNX является открытым стандартом представления моделей машинного обучения и обеспечивает аппаратно-независимый инференс: одна и та же модель может исполняться на различных вычислительных платформах без переобучения.

	\par \redline Конвертация выполняется стандартными средствами библиотеки Ultralytics по следующей команде:

	\formulaspace
	\begin{Center}
		\texttt{yolo export model=best.pt format=onnx imgsz=640 opset=17}
	\end{Center}
	\formulaspace

	\par \redline Параметр \texttt{opset=17} задаёт версию операционного набора ONNX, обеспечивающую совместимость с актуальными версиями среды выполнения ONNX Runtime. Входной тензор модели имеет форму $(B,\, 3,\, 640,\, 640)$, где $B$ -- размер пакета (batch size), при серверном инференсе $B = 1$.

	\par \redline Для дополнительного ускорения инференса на сервере применяется \textbf{ONNX Runtime} с оптимизацией вычислительного графа. Уровень оптимизации задаётся параметром
	\par \texttt{OrtOptimizationLevel.ORT\_ENABLE\_ALL}, что включает свёртку константных выражений, слияние матричных умножений и устранение неиспользуемых узлов вычислительного графа. Итоговая продолжительность инференса одного изображения на сервере с процессором Intel Xeon при использовании ONNX Runtime не превышает $t_{\mathrm{inf}} \leq 120$ мс, что обеспечивает приемлемое время отклика с учётом сетевых задержек.

	\par \redline Сравнение времени инференса модели в форматах PyTorch и ONNX Runtime представлено в таблице \thechaptercntr .\thetablecntr.

	\topTablespace
	{\begin{Center}
		\par Таблица \thechaptercntr .\thetablecntr \spc {--} Сравнение производительности форматов модели.

		\begin{tabular}{|l|c|c|c|}
			\hline
			\textbf{Формат} & \textbf{Размер файла, МБ} & \textbf{Время инференса, мс} & \textbf{mAP@0.5} \\ \hline
			PyTorch (.pt)   & 6,2                       & 185                          & исходный         \\ \hline
			ONNX            & 12,1                      & 117                          & исходный         \\ \hline
		\end{tabular}
	\end{Center}}
	\botTablespace \addtocounter{tablecntr}{1}

	\par \redline Как следует из таблицы, конвертация в формат ONNX позволяет снизить время инференса на $\approx 37\%$ без какого-либо ухудшения метрик точности модели, поскольку преобразование является строго детерминированным и не изменяет веса сети.

	\par \redline Дополнительно была исследована возможность квантизации модели в формат INT8 с целью дальнейшего снижения времени инференса и уменьшения требований к оперативной памяти. С помощью библиотеки onnxruntime-tools был проведён эксперимент: динамическая квантизация весов позволила сократить размер модели до 3,8 МБ, но точность mAP@0.5 упала на 4,2 процентных пункта, что признано неприемлемым для производственного аудита. Поэтому окончательно оставлена модель в формате ONNX с плавающей точкой FP32.

	\par \redline На рисунке \thechaptercntr .\theimagecntr приведён график зависимости времени инференса от размера пакета (batch size) для ONNX и PyTorch реализаций, иллюстрирующий преимущество ONNX Runtime при последовательной обработке одиночных изображений. Графики наглядно показывают, что ONNX Runtime обеспечивает минимальное время отклика именно в сценариях с низкой пакетной нагрузкой. Подобное поведение делает данный формат оптимальным выбором для развёртывания моделей в реальных production-средах, критичных к задержкам.

	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{images/inference_speed_batch.png}
		\caption*{\gostFont Рисунок \thechaptercntr .\theimagecntr \spc {--} Зависимость времени инференса от размера пакета.}
		\label{fig:inference_speed}
	\end{figure} \addtocounter{imagecntr}{1}

	\par
}

\subtitlespace

\subsection*{
	\gostTitleFont
	\redline
	\thechaptercntr .\thesubchaptercntr \spc
	Развёртывание Telegram-бота на базе aiogram
} \addtocounter{subchaptercntr}{1}

\subtitlespace

{\gostFont

	\par \redline Серверное приложение Telegram-бота реализовано на языке Python с использованием асинхронного фреймворка \textbf{aiogram 3.x}. Выбор aiogram обусловлен его нативной поддержкой асинхронного ввода-вывода (async/await), что позволяет обрабатывать несколько входящих сообщений одновременно без создания отдельных потоков операционной системы для каждого запроса.

	\par \redline Приложение бота обрабатывает входящие сообщения в соответствии с конечным автоматом состояний (FSM, Finite State Machine), реализованным средствами aiogram. Диаграмма состояний FSM включает следующие узлы:

	\begin{itemize}[leftmargin=2.15cm, labelwidth=0.65cm, labelsep=0.0cm]

		\item[\theitemcntr. ] \texttt{idle} -- ожидание команды или фотографии от пользователя.
		\addtocounter{itemcntr}{1}

		\item[\theitemcntr. ] \texttt{waiting\_photo} -- ожидание загрузки изображения полки.
		\addtocounter{itemcntr}{1}

		\item[\theitemcntr. ] \texttt{processing} -- выполнение инференса YOLOv8n и сравнение с планограммой.
		\addtocounter{itemcntr}{1}

		\item[\theitemcntr. ] \texttt{report\_sent} -- отправка аннотированного изображения и текстового отчёта пользователю.
		\addtocounter{itemcntr}{1}

	\end{itemize} \setcounter{itemcntr}{1}

	\par \redline Webhook регистрируется при запуске командой Telegram Bot API \texttt{setWebhook} с указанием HTTPS-URL сервера. Telegram требует использования протокола HTTPS с валидным SSL-сертификатом для регистрации webhook. В рамках развёртывания применяется бесплатный SSL-сертификат от центра сертификации \textbf{Let's Encrypt}, автоматически обновляемый утилитой Certbot.\par \redline Входящие HTTP-запросы от Telegram обрабатываются ASGI-сервером \textbf{Uvicorn}, принимающим соединения на порту 443. Nginx выступает в роли обратного прокси-сервера, терминирует TLS-соединение и перенаправляет расшифрованные запросы на Uvicorn для последующей маршрутизации внутри приложения.\par \redline Для защиты приложения от несанкционированных запросов и подмены данных на этапе приёма сообщений внедряется механизм валидации. При регистрации вебхука задаётся секретный токен, который Telegram передаёт в заголовке \texttt{X-Telegram-Bot-Api-Secret-Token} каждого HTTP-запроса. Приложение проверяет данный заголовок до начала парсинга тела запроса, и в случае несовпадения значений мгновенно отклоняет соединение с кодом \texttt{HTTP 403 Forbidden}, изолируя внутреннюю логику от вредоносного трафика.\par \redline Для обеспечения высокой отзывчивости системы и предотвращения повторной отправки пакетов со стороны Telegram (таймаут которого составляет 5 секунд), обработка запроса разделена на два этапа. При получении валидного JSON-пакета сервер мгновенно возвращает статус \texttt{HTTP 200 OK}, освобождая сетевое соединение. Сама же вычислительная процедура скачивания файла и инференса переносится в фоновый режим выполнения средствами асинхронной библиотеки \texttt{asyncio}.\par \redline Фоновая обработка входящего изображения включает следующую последовательность операций. Telegram передаёт на webhook-эндпоинт событие типа \texttt{Message} с вложением типа \texttt{PhotoSize}. Приложение загружает файл изображения через Telegram Bot API по уникальному \texttt{file\_id}. Загруженный файл сохраняется во временный буфер в памяти без записи на диск. Изображение передаётся в модуль инференса, где выполняется letterbox-масштабирование до $640 \times 640$ пикселей, нормализация и прямой проход через ONNX-модель.\par \redline Результаты детекции в виде набора пар (метка класса, координаты bounding box, confidence score) передаются в модуль сравнения с планограммой. Алгоритм сопоставляет координаты обнаруженных товаров с эталонной матрицей расположения продуктов на полке, вычисляя метрики перекрытия для выявления ошибок выкладки (out-of-stock). На основе собранной статистики формируется структурированный массив данных с итоговым процентом соответствия витрины регламенту.\par \redline Параллельно с расчётом аналитики промежуточные результаты выполнения операции фиксируются в локальной базе данных SQLite. Система сохраняет уникальный идентификатор пользователя, хэш обработанного изображения, временную метку, перечень обнаруженных SKU и рассчитанный коэффициент соответствия планограмме. Это позволяет накапливать ретроспективные данные для последующего построения отчётов и оптимизации скорости работы нейросетевого модуля за счёт мониторинга времени инференса.\par \redline Важным аспектом архитектуры является подсистема обеспечения отказоустойчивости и обработки исключительных ситуаций. При возникновении сетевых сбоев во время скачивания файла, превышении лимитов Telegram API или обнаружении некорректного формата входных данных процесс инференса изолированно прерывается блоком \texttt{try-except}. Вместо аварийного завершения фоновой задачи система генерирует лог-запись уровня \texttt{ERROR} и подготавливает понятное пользователю текстовое уведомление о возникшей ошибке.\par \redline Общая сквозная схема обработки webhook-запроса, отражающая сетевые, защитные и логические этапы, представлена на рисунке \thechaptercntr .\theimagecntr.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=0.35\textwidth]{images/webhook_flow.png}
		\caption*{\gostFont Рисунок \thechaptercntr .\theimagecntr \spc {--} Схема обработки webhook-запроса системой «Retail Vision».}
		\label{fig:webhook_flow}
	\end{figure} \addtocounter{imagecntr}{1}

	\par \redline Конфигурация приложения вынесена в файл \texttt{.env} и загружается при старте контейнера. Конфигурационный файл содержит следующие параметры:

	\begin{itemize}[leftmargin=2.15cm, labelwidth=0.65cm, labelsep=0.0cm]

		\item[•] \texttt{BOT\_TOKEN} -- токен доступа к Telegram Bot API.

		\item[•] \texttt{WEBHOOK\_URL} -- HTTPS-адрес публичного эндпоинта сервера.

		\item[•] \texttt{MODEL\_PATH} -- путь к файлу ONNX-модели внутри контейнера.

		\item[•] \texttt{PLANOGRAM\_DB\_PATH} -- путь к базе данных эталонных планограмм.

		\item[•] \texttt{CONF\_THRESHOLD} -- порог достоверности детекции (по умолчанию 0,25).

	\end{itemize}

	\par \redline Вынесение конфигурации в переменные окружения обеспечивает безопасность: токен бота не включается в исходный код и не попадает в систему контроля версий.



\subtitlespace

\subsection*{
	\gostTitleFont
	\redline
	\thechaptercntr .\thesubchaptercntr \spc
	Интеграция модуля сравнения с планограммой
} \addtocounter{subchaptercntr}{1}

\subtitlespace

{\gostFont

	\par \redline Ключевым функциональным модулем системы является \textbf{сравнение результатов детекции с эталонной планограммой}. Планограмма для каждой торговой точки хранится в базе данных в виде словаря, ключами которого являются идентификаторы SKU, а значениями -- плановое количество фейсингов каждой позиции.

	\par \redline Модуль анализа получает на вход список детектированных объектов (список пар класс -- confidence) и словарь эталонной планограммы. Для каждого класса из планограммы вычисляется метрика доступности товара на полке (On-Shelf Availability, OSA) по формуле (\thechaptercntr .\theformulacntr):

	\formulaspace \par \redline
		$\mathrm{OSA}_i = \min\!\left(1,\; \frac{n_i^{\mathrm{det}}}{n_i^{\mathrm{plan}}}\right) \times 100\%$
		\hfill (\thechaptercntr .\theformulacntr) \redline
	\formulaspace \addtocounter{formulacntr}{1}

	\begin{tabular}{p{0,875cm}p{0,3cm}p{15,175cm}}
		& где  & $n_i^{\mathrm{det}}$ -- количество детектированных фейсингов $i$-й позиции; \\
		&      & $n_i^{\mathrm{plan}}$ -- плановое количество фейсингов $i$-й позиции согласно планограмме. \\
	\end{tabular}

	\par \redline Итоговый показатель OSA по всей секции рассчитывается как среднее значение по всем $N_{\mathrm{cls}}$ позициям планограммы по формуле (\thechaptercntr .\theformulacntr):

	\formulaspace \par \redline
		$\mathrm{OSA}_{\mathrm{total}} = \frac{1}{N_{\mathrm{cls}}} \sum_{i=1}^{N_{\mathrm{cls}}} \mathrm{OSA}_i$
		\hfill (\thechaptercntr .\theformulacntr) \redline
	\formulaspace \addtocounter{formulacntr}{1}

	\par \redline Позиция признаётся нарушением планограммы, если выполняется условие $n_i^{\mathrm{det}} < n_i^{\mathrm{plan}}$. Для каждого нарушения фиксируется наименование SKU и количество отсутствующих фейсингов.

	\par \redline По результатам анализа формируется аннотированное изображение: на исходное фото накладываются bounding boxes, цвет которых кодирует статус позиции: зелёный -- позиция присутствует в достаточном количестве, красный -- позиция отсутствует или её количество ниже планового. Аннотация выполняется средствами библиотеки \textbf{OpenCV} с использованием функции \texttt{cv2.rectangle} для прямоугольников и \texttt{cv2.putText} для подписей классов.

	\par \redline Текстовая часть отчёта включает итоговое значение OSA в процентах, перечень выявленных нарушений с указанием SKU и количества недостающих фейсингов, а также временну́ю метку проведения аудита. Отчёт формируется в формате Markdown и отправляется пользователю вместе с аннотированным изображением одним сообщением через метод \texttt{send\_photo} Telegram Bot API.

	\par \redline Для обеспечения корректной работы при большом количестве SKU в планограмме (более 50 позиций) модуль сравнения кэширует результаты сопоставления имён классов между детекцией и базой данных. Поскольку модель YOLOv8n выдаёт предсказания в виде числовых индексов классов, используется статический маппинг из конфигурационного файла. Администратор системы может обновить планограмму без перезапуска бота: изменения автоматически подхватываются при следующем запросе благодаря механизму перезагрузки БД по сигналу SIGHUP.

	\par
}

\subtitlespace

\subsection*{
	\gostTitleFont
	\redline
	\thechaptercntr .\thesubchaptercntr \spc
	Контейнеризация и конфигурация серверного окружения
} \addtocounter{subchaptercntr}{1}

\subtitlespace

{\gostFont

	\par \redline Система «Retail Vision» развёртывается в виде набора Docker-контейнеров, управляемых с помощью Docker Compose. Многоконтейнерная архитектура позволяет изолировать каждый компонент системы, независимо масштабировать его при необходимости и упростить процедуру обновления отдельных модулей без перезапуска всей системы.

	\par \redline Состав сервисов в файле \texttt{docker-compose.yml} включает следующие контейнеры:

	\begin{itemize}[leftmargin=2.15cm, labelwidth=0.65cm, labelsep=0.0cm]

		\item[\theitemcntr. ] \texttt{bot} -- основной контейнер приложения aiogram с подключённым ONNX Runtime.
		\addtocounter{itemcntr}{1}

		\item[\theitemcntr. ] \texttt{nginx} -- контейнер обратного прокси-сервера с SSL-терминацией.
		\addtocounter{itemcntr}{1}

		\item[\theitemcntr. ] \texttt{db} -- контейнер базы данных SQLite с томом для персистентного хранения планограмм.
		\addtocounter{itemcntr}{1}

	\end{itemize} \setcounter{itemcntr}{1}

	\par \redline Dockerfile основного контейнера \texttt{bot} основан на образе \texttt{python:3.11-slim} для минимизации его итогового размера. Зависимости устанавливаются через \texttt{pip} из файла \texttt{requirements.txt}. ONNX-модель копируется в образ при сборке командой \texttt{COPY}. Точкой входа (entrypoint) является запуск Uvicorn с параметрами хоста и порта.

	\par \redline Технические характеристики минимально необходимой конфигурации VPS-сервера для стабильной работы системы приведены в таблице \thechaptercntr .\thetablecntr.

	\topTablespace
	{\begin{Center}
		\par Таблица \thechaptercntr .\thetablecntr \spc {--} Минимальные требования к конфигурации сервера.

		\begin{tabular}{|l|l|}
			\hline
			\textbf{Параметр}       & \textbf{Значение}                   \\ \hline
			Процессор               & 2 vCPU (x86-64), $\geq 2,0$ ГГц    \\ \hline
			Оперативная память      & 2 ГБ RAM                            \\ \hline
			Дисковое пространство   & 20 ГБ SSD                           \\ \hline
			Операционная система    & Ubuntu 22.04 LTS (64-bit)           \\ \hline
			Сетевой интерфейс       & 100 Мбит/с, публичный IPv4-адрес   \\ \hline
		\end{tabular}
	\end{Center}}
	\botTablespace \addtocounter{tablecntr}{1}

	\par \redline Процесс первичного развёртывания системы на VPS-сервере состоит из следующих шагов:

	\begin{itemize}[leftmargin=2.15cm, labelwidth=0.65cm, labelsep=0.0cm]

		\item[\theitemcntr. ] Установка Docker Engine и Docker Compose из официального репозитория.
		\addtocounter{itemcntr}{1}

		\item[\theitemcntr. ] Получение SSL-сертификата для домена сервера с помощью Certbot: \texttt{certbot certonly --standalone -d <домен>}.
		\addtocounter{itemcntr}{1}

		\item[\theitemcntr. ] Клонирование репозитория проекта и заполнение файла \texttt{.env} с конфигурационными параметрами.
		\addtocounter{itemcntr}{1}

		\item[\theitemcntr. ] Сборка Docker-образов и запуск всех контейнеров командой \texttt{docker compose up --build -d}.
		\addtocounter{itemcntr}{1}

		\item[\theitemcntr. ] Верификация регистрации webhook через Telegram Bot API \texttt{getWebhookInfo}.
		\addtocounter{itemcntr}{1}

	\end{itemize} \setcounter{itemcntr}{1}

	\par \redline После успешного выполнения всех шагов система переходит в рабочее состояние: Telegram регистрирует webhook и начинает доставлять входящие обновления на указанный HTTPS-эндпоинт. Логи работы каждого контейнера доступны через команду \texttt{docker compose logs -f <имя\_сервиса>}, что обеспечивает возможность оперативного мониторинга и диагностики.

	\par \redline Для обеспечения автоматического перезапуска контейнеров в случае сбоя в файле docker-compose.yml задана политика \texttt{restart: unless-stopped}. Дополнительно настроен healthcheck для контейнера \texttt{bot}: каждые 30 секунд uvicorn отвечает на GET-запрос по эндпоинту \texttt{/health}, возвращая статус 200, если модель загружена и БД доступна. При трёх последовательных неудачных проверках Docker перезапускает контейнер.

	\par
}

\subtitlespace

\subsection*{
	\gostTitleFont
	\redline
	\thechaptercntr .\thesubchaptercntr \spc
	Тестирование развёрнутой системы
} \addtocounter{subchaptercntr}{1}

\subtitlespace

{\gostFont

	\par \redline После развёртывания системы на VPS-сервере выполняется её комплексное тестирование в условиях, приближенных к реальной эксплуатации. Тестирование проводится по двум направлениям: функциональному и нагрузочному.

	\par \redline \textbf{Функциональное тестирование} проверяет корректность каждого сценария взаимодействия пользователя с ботом. Тестовые сценарии включают:

	\begin{itemize}[leftmargin=2.15cm, labelwidth=0.65cm, labelsep=0.0cm]

		\item[\theitemcntr. ] Отправку корректного изображения полки с продукцией Санта Импэкс: ожидаемый результат -- аннотированное фото и текстовый отчёт с метриками OSA в течение не более 10 секунд.
		\addtocounter{itemcntr}{1}

		\item[\theitemcntr. ] Отправку изображения полки без продукции Санта Импэкс (посторонний товар): ожидаемый результат -- сообщение об отсутствии распознанных SKU.
		\addtocounter{itemcntr}{1}

		\item[\theitemcntr. ] Отправку файла неподдерживаемого формата (документ, аудио): ожидаемый результат -- информационное сообщение с просьбой отправить фотографию.
		\addtocounter{itemcntr}{1}

		\item[\theitemcntr. ] Отправку заведомо некорректного изображения (полностью чёрный кадр, разрешение менее $100 \times 100$ пикселей): ожидаемый результат -- корректная обработка без сбоя сервера.
		\addtocounter{itemcntr}{1}

	\end{itemize} \setcounter{itemcntr}{1}

	\par \redline Все перечисленные сценарии выполнены успешно. Система корректно обрабатывает граничные случаи, не допуская неперехваченных исключений, которые могли бы привести к остановке процесса.

	\par \redline \textbf{Нагрузочное тестирование} моделирует одновременную отправку изображений от нескольких пользователей с помощью инструмента \textbf{Locust}. В ходе тестирования проверялась стабильность системы при 10 одновременных пользователях, каждый из которых с интервалом 30 секунд отправлял изображение. Результаты нагрузочного тестирования представлены в таблице \thechaptercntr .\thetablecntr.
	
	\subtitlespace

	\topTablespace
	{\begin{Center}
		\par Таблица \thechaptercntr .\thetablecntr \spc {--} Результаты нагрузочного тестирования системы.

		\begin{tabular}{|l|c|}
			\hline
			\textbf{Показатель}                           & \textbf{Значение} \\ \hline
			Количество одновременных пользователей        & 10                \\ \hline
			Среднее время ответа системы, мс              & 3\,840            \\ \hline
			95-й перцентиль времени ответа, мс            & 5\,210            \\ \hline
			Максимальное время ответа, мс                 & 7\,480            \\ \hline
			Доля успешных ответов (без ошибок HTTP 5xx), \% & 100,0           \\ \hline
			Потребление RAM контейнером \texttt{bot}, МБ  & 480               \\ \hline
			Загрузка CPU, \%                              & 67                \\ \hline
		\end{tabular}
	\end{Center}}
	\botTablespace \addtocounter{tablecntr}{1}

	\par \redline Результаты тестирования подтверждают стабильность системы при нагрузке 10 одновременных пользователей: доля успешных ответов составила 100\%, максимальное время ответа не превысило 7,5 секунды, что является приемлемым показателем для сценария аудита полки. В процессе тестирования зафиксировано пиковое потребление оперативной памяти 480 МБ, что не превышает выделенного лимита в 2 ГБ.

	\par \redline Дополнительно проведено тестирование на долговременную стабильность (soak test) в течение 72 часов с интенсивностью 1 запрос в минуту. За это время не зафиксировано утечек памяти или деградации производительности: среднее время ответа оставалось в пределах 3,9–4,1 секунды. Мониторинг ресурсов осуществлялся через cAdvisor; визуализация метрик показала равномерное потребление CPU без «пилообразных» выбросов.

	\par
}

\subtitlespace

\subsection*{
	\gostTitleFont
	\redline
	\thechaptercntr .\thesubchaptercntr \spc
	Безопасность развёртывания и управление секретами
} \addtocounter{subchaptercntr}{1}

\subtitlespace

{\gostFont

	\par \redline В производственном окружении безопасность системы «Retail Vision» обеспечивается на нескольких уровнях: сетевом, прикладном и на уровне хранения конфиденциальных данных. Основной акцент сделан на защите токена Telegram-бота, SSL-сертификатов и целостности базы данных планограмм.

	\par \redline Токен бота (\texttt{BOT\_TOKEN}) является критическим секретом: его компрометация позволяет злоумышленнику управлять ботом от имени организации. Для защиты токен не включается в исходный код и не хранится в файлах Docker-образов. Вместо этого он передаётся в контейнер через файл \texttt{.env}, который исключён из системы контроля версий с помощью записи в \texttt{.gitignore}. На сервере файл \texttt{.env} имеет права доступа 600 (чтение и запись только для владельца), а сам контейнер настроен на чтение переменных окружения из этого файла при запуске. Дополнительно в production-среде рекомендуется использовать менеджер секретов (например, HashiCorp Vault или встроенный Docker Secrets), однако в текущей версии системы принят компромиссный вариант с защищённым файлом \texttt{.env} ввиду малого масштаба развёртывания.

	\par \redline SSL-сертификаты, полученные от Let's Encrypt, хранятся в отдельном Docker-томе, монтируемом только в контейнер Nginx. Контейнер бота не имеет доступа к закрытым ключам, что снижает риск их утечки при возможной компрометации прикладного уровня. Автоматическое обновление сертификатов выполняется раз в 60 дней через cron-задачу на хосте, которая после обновления отправляет сигнал перезагрузки Nginx.

	\par \redline На сетевом уровне VPS-сервер защищён встроенным файрволом UFW. Разрешены только входящие соединения на порты 22 (SSH, с ограничением по ключам и запретом парольной аутентификации), 80 (HTTP, для redirect на HTTPS) и 443 (HTTPS, основной webhook). Все остальные порты закрыты. Для дополнительной защиты от DDoS-атак на webhook-эндпоинт Nginx настроен на ограничение частоты запросов: не более 10 запросов в секунду с одного IP-адреса. Превышение лимита приводит к временной блокировке на 60 секунд с возвратом кода 429.

	\par \redline Доступ к базе данных планограмм (SQLite) осуществляется только из контейнера \texttt{bot} через внутреннюю сеть Docker. Файл базы данных размещён на томе, который не экспортируется наружу. Таким образом, даже при получении доступа к серверу злоумышленник не может изменить планограммы без прямой атаки на контейнер. Для дополнительной защиты данных при передаче от администратора к системе обновление планограмм выполняется через защищённое SSH-соединение с последующей перезагрузкой БД по сигналу SIGHUP.

	\par \redline Все описанные меры соответствуют принципу наименьших привилегий и рекомендациям OWASP для развёртывания веб-приложений. Регулярный аудит безопасности включает проверку версий Docker-образов на наличие известных уязвимостей с помощью \texttt{docker scan} (на базе Snyk). На момент написания критические уязвимости не обнаружены.

	\par
}

\subtitlespace

\subsection*{
	\gostTitleFont
	\redline
	\thechaptercntr .\thesubchaptercntr \spc
	Мониторинг, логирование и оповещение
} \addtocounter{subchaptercntr}{1}

\subtitlespace

{\gostFont

	\par \redline Для обеспечения надёжной эксплуатации системы «Retail Vision» настроен централизованный сбор метрик, логов и алертов. В качестве базового инструмента мониторинга используется связка \textbf{Prometheus} + \textbf{Grafana}, развёрнутая на том же VPS-сервере в дополнительных Docker-контейнерах. Хотя это увеличивает потребление ресурсов (≈256 МБ RAM и 0.5 vCPU), преимущества в виде наглядной визуализации и раннего обнаружения проблем признаны оправданными.

	\par \redline Основные метрики, собираемые с контейнера \texttt{bot}:
	\begin{itemize}
		\item количество обработанных webhook-запросов в минуту;
		\item гистограмма времени ответа (перцентили: p50, p95, p99);
		\item частота ошибок HTTP 4xx и 5xx;
		\item загрузка CPU и потребление памяти контейнером;
		\item время инференса модели ONNX;
		\item количество активных пользователей в состоянии FSM.
	\end{itemize}
	Экспорт метрик реализован через библиотеку \texttt{prometheus\_client} для Python, которая добавляет эндпоинт \texttt{/metrics} к серверу Uvicorn. Prometheus настроен на сбор метрик каждые 15 секунд.

	\par \redline Логирование осуществляется в формате JSON (через библиотеку \texttt{python-json-logger}), что упрощает последующий парсинг и анализ. Каждая запись лога содержит временную метку, уровень (INFO, WARNING, ERROR), имя модуля, идентификатор пользователя (telegram ID) и краткое сообщение. Логи из stdout контейнера автоматически собираются драйвером Docker и могут быть просмотрены командой \texttt{docker compose logs}. Для долговременного хранения и ротации логов настроен \textbf{ELK Stack} (Elasticsearch, Logstash, Kibana) в минимальной конфигурации. Из-за ограничений ресурсов VPS хранение логов ограничено 14 днями, после чего выполняется автоматическая ротация.

	\par \redline Система оповещения построена на базе правил Prometheus \texttt{Alertmanager}. Настроены следующие критические алерты:
	\begin{itemize}
		\item контейнер \texttt{bot} или \texttt{nginx} не работает более 1 минуты;
		\item 95-й перцентиль времени ответа превышает 10 секунд в течение 5 минут;
		\item частота ошибок HTTP 5xx выше 5\% за 5 минут;
		\item загрузка CPU контейнера превышает 85\% в течение 10 минут;
		\item свободное место на диске менее 10\%.
	\end{itemize}
	При срабатывании алерта отправляется уведомление в Telegram-чат администраторов через отдельного бота (не того, который выполняет аудит). Также предусмотрена интеграция с PagerDuty для ночных вызовов при недоступности системы более 5 минут.

	\par \redline Визуализация метрик осуществляется в дашборде Grafana, который содержит панели с графиками времени ответа, количества запросов, ошибок и использования ресурсов. Дашборд доступен по защищённому HTTPS-адресу с базовой аутентификацией. Такой подход позволяет оперативно реагировать на аномалии и проводить пост-инцидентный анализ.

	\par
}

\subtitlespace

\subsection*{
	\gostTitleFont
	\redline
	\thechaptercntr .\thesubchaptercntr \spc
	Резервное копирование и восстановление планограмм
} \addtocounter{subchaptercntr}{1}

\subtitlespace

{\gostFont

	\par \redline База данных планограмм является критически важным компонентом системы: при её повреждении или случайном удалении все торговые точки останутся без эталонных значений, что сделает аудит невозможным. Для предотвращения потери данных реализован многоуровневый механизм резервного копирования.

	\par \redline Такой подход обеспечивает возможность восстановления базы данных на любой момент в пределах последних 30 дней. Объём одного архива не превышает 100 МБ, так как планограммы представляют собой небольшой JSON-словарь.

	\par \redline Для тестирования процедуры восстановления еженедельно (по воскресеньям) выполняется автоматический сценарий: система запускает тестовый контейнер с чистым томом, восстанавливает последний бэкап и проверяет целостность базы данных с помощью встроенных средств SQLite (\texttt{PRAGMA integrity\_check}). Отчёт о результате восстановления отправляется в тот же канал администраторов.

	\par \redline Дополнительно реализовано точечное резервное копирование перед каждым обновлением планограммы (через API администратора). При получении команды на изменение планограммы система автоматически создаёт копию текущей БД с суффиксом \texttt{\_before\_update\_YYYYMMDDHHMMSS.db}. Это позволяет откатить ошибочное изменение без обращения к ежедневному бэкапу.

	\par \redline Восстановление из бэкапа выполняется вручную командой администратора. Процесс включает остановку контейнера \texttt{bot}, замену файла \texttt{planograms.db} на томе, перезапуск контейнера и отправку тестового запроса для верификации. Среднее время восстановления (RTO) составляет не более 15 минут, целевая точка восстановления (RPO) -- не более 24 часов (при ежедневном бэкапе) или несколько минут (при точечном перед изменением).

	\par \redline Таким образом, система «Retail Vision» готова к производственной эксплуатации с точки зрения сохранности данных и возможности восстановления после сбоев.

	\par
}

\subtitlespace

{\gostFont

	\par \redline В ходе экспериментальной проверки и тестирования разработанного программного обеспечения были зафиксированы количественные показатели точности распознавания товаров и скорости работы нейросетевого модуля. Оптимизированная модель YOLOv8n в формате ONNX продемонстрировала среднюю точность детекции ($mAP_{50}$) на уровне 88–92\%, что полностью удовлетворяет практическим требованиям автоматизированного контроля планограмм для целевых групп товаров «Санта Импэкс». При этом среднее время инференса на один кадр в условиях последовательной обработки одиночных изображений не превысило допустимых лимитов, подтверждая высокую эффективность выбранного runtime-движка.\par \redline Интеграция вебхук-эндпоинтов с обратным прокси-сервером и сервером Uvicorn позволила добиться высокой отказоустойчивости при обработке входящего трафика от Telegram API. Применение асинхронного подхода на базе библиотеки \texttt{asyncio} для выноса тяжёлых вычислительных задач в фоновый режим исключило риск возникновения сетевых таймаутов. Локальная база данных SQLite успешно справилась с фиксацией промежуточных метрик детекции и логов, подтвердив стабильность разработанной схемы данных при имитации одновременных запросов от нескольких активных мерчендайзеров.\par \redline Внедрение разработанного алгоритма сопоставления реальной выкладки с эталонной матрицей расположения продуктов позволило значительно сократить время рутинного аудита торговой точки и снизить влияние человеческого фактора. Накопленная в СУБД ретроспективная статистика закладывает технологический фундамент для дальнейшего масштабирования проекта, включая переход на более мощные архитектуры нейросетей и миграцию на распределённые базы данных для поддержки региональной сети филиалов.\par \redline Таким образом, выполненный комплекс работ по развёртыванию системы «Retail Vision» подтвердил её готовность к производственной эксплуатации. Система обеспечивает автоматизированный аудит выкладки продукции Санта Импэкс в торговых точках с использованием обученной модели YOLOv8n, предоставляет мерчендайзеру наглядный отчёт через интерфейс Telegram и демонстрирует устойчивую работу при многопользовательской нагрузке.
	\par
}

\setcounter{subchaptercntr}{1}
\setcounter{formulacntr}{1}
\setcounter{imagecntr}{1}
\setcounter{tablecntr}{1}
\setcounter{itemcntr}{1}